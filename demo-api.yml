spring:
  cloud:
    name: demo-api

  boot:
    admin:
      client:
        url: http://localhost:1111

management:
  endpoints:
    web:
      exposure:
        include: "*"

message: "Welcome to the world of Microservices"
